const fs = require("fs-extra");

fs.writeJsonSync("./data.json", []);

// let obj = fs.readJsonSync("./data.json");

// console.log(obj);

// obj.push({ msg: "msg" });

// fs.writeJsonSync("./data.json", obj);

// obj = fs.readJsonSync("./data.json");

// console.log(obj);
